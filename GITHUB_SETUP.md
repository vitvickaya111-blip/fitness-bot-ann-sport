# 🚀 Быстрая настройка GitHub Actions

## 1️⃣ Генерация SSH ключа

На вашем локальном компьютере:

```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "github-actions@fitness-bot" -f ~/.ssh/fitness-bot-deploy

# Копирование публичного ключа на сервер
ssh-copy-id -i ~/.ssh/fitness-bot-deploy.pub root@78.140.241.105

# Проверка подключения
ssh -i ~/.ssh/fitness-bot-deploy root@78.140.241.105
```

## 2️⃣ Добавление секрета в GitHub

1. Скопируйте приватный ключ:
```bash
cat ~/.ssh/fitness-bot-deploy
```

2. Перейдите в ваш GitHub репозиторий:
   - **Settings** → **Secrets and variables** → **Actions**

3. Нажмите **New repository secret**

4. Заполните:
   - **Name:** `SSH_PRIVATE_KEY`
   - **Value:** Вставьте весь вывод команды (включая `-----BEGIN` и `-----END`)

5. Нажмите **Add secret**

## 3️⃣ Подготовка сервера

Подключитесь к серверу и выполните:

```bash
# Подключение к серверу
ssh root@78.140.241.105

# Создание директории
mkdir -p /home/fitness-bot-ann-sport
cd /home/fitness-bot-ann-sport

# Клонирование репозитория (если еще не сделано)
git clone https://github.com/your-username/fitness-bot-ann-sport.git .

# Создание .env файла
nano .env
```

Вставьте в `.env`:
```env
BOT_TOKEN=8380404463:AAHYJnUD5h19Ffc4v01x0m1mnBlzwKtNjpw
ADMIN_ID=1258139980
STUDIO_NAME=FitStudio
STUDIO_ADDRESS=пр. Комсомольский 3 (второй этаж)
CARD_NUMBER=2200700123456789
CARD_HOLDER=MARIA IVANOVA
CHANNEL_USERNAME=@OFFICIAL_AN_SPORT
ENVIRONMENT=production
DATABASE_URL=sqlite+aiosqlite:////app/data/bot.db
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

## 4️⃣ Первый запуск

```bash
# Запуск контейнеров
docker compose up -d

# Проверка статуса
docker compose ps

# Просмотр логов
docker logs -f fitness-bot
```

**Dozzle** будет доступен: http://78.140.241.105:8080

## 5️⃣ Тестирование автодеплоя

На вашем локальном компьютере:

```bash
# Внесите изменение (например, в README)
echo "# Test deploy" >> test.txt
git add test.txt
git commit -m "test: testing CI/CD"
git push origin main
```

Проверьте деплой:
1. Откройте GitHub → **Actions**
2. Следите за процессом деплоя
3. При успехе бот автоматически обновится на сервере

## ✅ Готово!

Теперь при каждом push в `main` бот будет автоматически:
- ✅ Собираться в Docker образ
- ✅ Публиковаться в GitHub Container Registry
- ✅ Деплоиться на сервер 78.140.241.105
- ✅ Перезапускаться без downtime

## 🔍 Мониторинг

- **Логи бота:** `docker logs -f fitness-bot`
- **Dozzle UI:** http://78.140.241.105:8080
- **GitHub Actions:** https://github.com/your-username/fitness-bot-ann-sport/actions

## 🆘 Troubleshooting

### SSH подключение не работает
```bash
# Проверьте права на ключ
chmod 600 ~/.ssh/fitness-bot-deploy

# Попробуйте подключиться вручную
ssh -i ~/.ssh/fitness-bot-deploy root@78.140.241.105
```

### GitHub Actions не может подключиться
1. Проверьте, что секрет `SSH_PRIVATE_KEY` правильно добавлен
2. Убедитесь, что публичный ключ добавлен на сервер
3. Проверьте, что порт 22 открыт на сервере

### Контейнер не запускается
```bash
# Проверьте логи
docker logs fitness-bot

# Проверьте .env файл
cat .env

# Пересоздайте контейнеры
docker compose down -v
docker compose up -d
```
